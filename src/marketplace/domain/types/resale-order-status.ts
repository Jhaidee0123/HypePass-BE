export enum ResaleOrderStatus {
    PENDING = 'pending',
    PAID = 'paid',
    FAILED = 'failed',
    CANCELLED = 'cancelled',
    EXPIRED = 'expired',
    SETTLED = 'settled',
    REFUNDED = 'refunded',
}
